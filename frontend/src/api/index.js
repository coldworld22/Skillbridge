// # 📡 Optional: API route builders or fetchers (if needed per module)
// │   ├── index.js            # Combines endpoints if needed
// │   └── endpoints.js        # Exports full endpoint paths