# Security Notes

Run `npm audit --production` in `frontend` and `backend` to check for known vulnerabilities. As of this update, the frontend reports issues with `form-data`, `next`, `prismjs`, and `quill` packages.
